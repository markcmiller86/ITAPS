# Don't require GNU-standard files (Changelog, README, etc.)
AUTOMAKE_OPTIONS = foreign

# Don't lose the autoconf include path
ACLOCAL_AMFLAGS = -I m4

SUBDIRS = 

if HAVE_IMESH
  SUBDIRS += IMESH
endif

